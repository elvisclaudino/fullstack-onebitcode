let velocidade = 40;

do {
  alert("A velocidade do veículo é de " + velocidade + " km/h");
  velocidade -= 20;
} while (velocidade > 0);

alert("A velocidade final do veículo é de " + velocidade + " km/h");
